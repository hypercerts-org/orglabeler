/*
 * THIS FILE WAS GENERATED BY "@atproto/lex". DO NOT EDIT.
 */

import { l } from '@atproto/lex'
import * as RepoStrongRef from '../../../com/atproto/repo/strongRef.defs'
import * as CertifiedDefs from '../../../app/certified/defs.defs'
import * as HypercertsDefs from '../defs.defs'

const $nsid = 'org.hypercerts.context.evaluation'

export { $nsid }

/** An evaluation of a hypercert record (e.g. an activity and its impact). */
type Main = {
  $type: 'org.hypercerts.context.evaluation'

  /**
   * A strong reference to what is being evaluated (e.g. activity, measurement, contribution, etc.)
   */
  subject?: RepoStrongRef.Main

  /**
   * DIDs of the evaluators
   */
  evaluators: CertifiedDefs.Did[]

  /**
   * Evaluation data (URIs or blobs) containing detailed reports or methodology
   */
  content?: (
    | l.$Typed<HypercertsDefs.Uri>
    | l.$Typed<HypercertsDefs.SmallBlob>
    | l.Unknown$TypedObject
  )[]

  /**
   * Optional references to the measurements that contributed to this evaluation. The record(s) referenced must conform with the lexicon org.hypercerts.context.measurement
   */
  measurements?: RepoStrongRef.Main[]

  /**
   * Brief evaluation summary
   */
  summary: string

  /**
   * Optional overall score for this evaluation on a numeric scale.
   */
  score?: Score

  /**
   * An optional reference for georeferenced evaluations. The record referenced must conform with the lexicon app.certified.location.
   */
  location?: RepoStrongRef.Main

  /**
   * Client-declared timestamp when this record was originally created
   */
  createdAt: l.DatetimeString
}

export type { Main }

/** An evaluation of a hypercert record (e.g. an activity and its impact). */
const main = l.record<'tid', Main>(
  'tid',
  $nsid,
  l.object({
    subject: l.optional(
      l.ref<RepoStrongRef.Main>((() => RepoStrongRef.main) as any),
    ),
    evaluators: l.array(
      l.ref<CertifiedDefs.Did>((() => CertifiedDefs.did) as any),
      { maxLength: 1000 },
    ),
    content: l.optional(
      l.array(
        l.typedUnion(
          [
            l.typedRef<HypercertsDefs.Uri>((() => HypercertsDefs.uri) as any),
            l.typedRef<HypercertsDefs.SmallBlob>(
              (() => HypercertsDefs.smallBlob) as any,
            ),
          ],
          false,
        ),
        { maxLength: 100 },
      ),
    ),
    measurements: l.optional(
      l.array(l.ref<RepoStrongRef.Main>((() => RepoStrongRef.main) as any), {
        maxLength: 100,
      }),
    ),
    summary: l.string({ maxLength: 5000, maxGraphemes: 1000 }),
    score: l.optional(l.ref<Score>((() => score) as any)),
    location: l.optional(
      l.ref<RepoStrongRef.Main>((() => RepoStrongRef.main) as any),
    ),
    createdAt: l.string({ format: 'datetime' }),
  }),
)

export { main }

export const $isTypeOf = /*#__PURE__*/ main.isTypeOf.bind(main),
  $build = /*#__PURE__*/ main.build.bind(main),
  $type = /*#__PURE__*/ main.$type
export const $assert = /*#__PURE__*/ main.assert.bind(main),
  $check = /*#__PURE__*/ main.check.bind(main),
  $cast = /*#__PURE__*/ main.cast.bind(main),
  $ifMatches = /*#__PURE__*/ main.ifMatches.bind(main),
  $matches = /*#__PURE__*/ main.matches.bind(main),
  $parse = /*#__PURE__*/ main.parse.bind(main),
  $safeParse = /*#__PURE__*/ main.safeParse.bind(main),
  $validate = /*#__PURE__*/ main.validate.bind(main),
  $safeValidate = /*#__PURE__*/ main.safeValidate.bind(main)

/** Overall score for an evaluation on a numeric scale. */
type Score = {
  $type?: 'org.hypercerts.context.evaluation#score'

  /**
   * Minimum value of the scale, e.g. 0 or 1.
   */
  min: number

  /**
   * Maximum value of the scale, e.g. 5 or 10.
   */
  max: number

  /**
   * Score within the inclusive range [min, max].
   */
  value: number
}

export type { Score }

/** Overall score for an evaluation on a numeric scale. */
const score = l.typedObject<Score>(
  $nsid,
  'score',
  l.object({ min: l.integer(), max: l.integer(), value: l.integer() }),
)

export { score }
